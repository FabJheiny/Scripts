-- ScriptHub.lua | UNIVERSAL HUB (Delta Fix)
local Hub = {}
local LP = game:GetService("Players").LocalPlayer
local UIS = game:GetService("UserInputService")
local PG = LP:WaitForChild("PlayerGui")
local B  = Color3.fromRGB(0,0,0)
local W  = Color3.fromRGB(255,255,255)
local D  = Color3.fromRGB(15,15,15)
local G  = Color3.fromRGB(50,50,50)
local DT = Color3.fromRGB(180,180,180)

local vp  = workspace.CurrentCamera.ViewportSize
local _sc = math.min(1, math.min(vp.X / 1366, vp.Y / 768))
local winW = math.floor(680 * _sc)
local winH = math.floor(310 * _sc)

local function o(cls,props,par)
    local x=Instance.new(cls)
    for k,v in pairs(props) do x[k]=v end
    if par then x.Parent=par end
    return x
end
local function stroke(par,t,c)
    o("UIStroke",{Color=c or W,Thickness=t or 1,ApplyStrokeMode=Enum.ApplyStrokeMode.Border},par)
end

local sg = o("ScreenGui",{Name="UniversalHub",ResetOnSpawn=false,IgnoreGuiInset=true,DisplayOrder=100},PG)

local win = o("Frame",{
    Size=UDim2.new(0,winW,0,winH),
    Position=UDim2.new(.5,-winW/2,.5,-winH/2),
    BackgroundColor3=B,BorderSizePixel=0,
    Active=true,ClipsDescendants=true,Visible=true,
    Draggable=true,
},sg)
stroke(win,1)

local top = o("Frame",{Size=UDim2.new(1,0,0,24),BackgroundColor3=B,BorderSizePixel=0,Active=false},win)
o("Frame",{Size=UDim2.new(1,0,0,1),Position=UDim2.new(0,0,1,-1),BackgroundColor3=G,BorderSizePixel=0},top)
o("TextLabel",{
    Size=UDim2.new(1,-60,1,0),Position=UDim2.new(0,8,0,0),
    BackgroundTransparency=1,Text="> UNIVERSAL HUB <",TextColor3=W,
    TextSize=14,Font=Enum.Font.SourceSansBold,TextXAlignment=Enum.TextXAlignment.Left,
},top)
local minBtn = o("TextButton",{
    Size=UDim2.new(0,18,0,14),Position=UDim2.new(1,-40,0.5,-7),
    BackgroundColor3=B,BorderSizePixel=0,Text="-",TextColor3=W,
    TextSize=13,Font=Enum.Font.SourceSansBold,AutoButtonColor=false,
},top)
local xBtn = o("TextButton",{
    Size=UDim2.new(0,18,0,14),Position=UDim2.new(1,-20,0.5,-7),
    BackgroundColor3=B,BorderSizePixel=0,Text="x",TextColor3=W,
    TextSize=10,Font=Enum.Font.SourceSansBold,AutoButtonColor=false,
},top)

local mini=false local fullH,miniH=winH,24
minBtn.MouseButton1Click:Connect(function()
    mini=not mini
    win.Size=UDim2.new(0,winW,0,mini and miniH or fullH)
    minBtn.Text=mini and "+" or "-"
end)
xBtn.MouseButton1Click:Connect(function()
    win.Visible=false
    _G.UniversalHubPill.Visible=true
end)

local body = o("Frame",{
    Size=UDim2.new(1,0,1,-24),Position=UDim2.new(0,0,0,24),
    BackgroundColor3=B,BorderSizePixel=0,ClipsDescendants=true,
},win)

local tcol = o("Frame",{Size=UDim2.new(0,90,1,0),BackgroundColor3=B,BorderSizePixel=0},body)
o("Frame",{Size=UDim2.new(0,1,1,0),Position=UDim2.new(1,0,0,0),BackgroundColor3=G,BorderSizePixel=0},tcol)
local tlist = o("ScrollingFrame",{
    Size=UDim2.new(1,0,1,0),BackgroundTransparency=1,BorderSizePixel=0,
    ScrollBarThickness=0,CanvasSize=UDim2.new(0,0,0,0),AutomaticCanvasSize=Enum.AutomaticSize.Y,
},tcol)
o("UIListLayout",{SortOrder=Enum.SortOrder.LayoutOrder},tlist)

local area = o("Frame",{
    Size=UDim2.new(1,-91,1,0),Position=UDim2.new(0,91,0,0),
    BackgroundColor3=B,BorderSizePixel=0,ClipsDescendants=true,
},body)

-- ── Modal ─────────────────────────────────────────────────────────────
local modal = o("Frame",{
    Size=UDim2.new(1,0,1,0),
    BackgroundColor3=Color3.fromRGB(0,0,0),
    BackgroundTransparency=0.45,
    BorderSizePixel=0,Visible=false,ZIndex=50,
},sg)
local modalBg = o("TextButton",{
    Size=UDim2.new(1,0,1,0),BackgroundTransparency=1,Text="",ZIndex=51,
},modal)

local mbox = o("Frame",{
    Size=UDim2.new(0,220,0,90),
    Position=UDim2.new(.5,-110,.5,-45),
    BackgroundColor3=B,BorderSizePixel=0,ZIndex=52,
},modal)
stroke(mbox,1)

local mTitle = o("TextLabel",{
    Size=UDim2.new(1,0,0,28),Position=UDim2.new(0,0,0,0),
    BackgroundColor3=Color3.fromRGB(10,10,10),BorderSizePixel=0,
    Text="",TextColor3=W,TextSize=11,Font=Enum.Font.SourceSansBold,
    TextXAlignment=Enum.TextXAlignment.Center,TextWrapped=true,ZIndex=53,
},mbox)
o("Frame",{Size=UDim2.new(1,0,0,1),Position=UDim2.new(0,0,1,-1),BackgroundColor3=G,BorderSizePixel=0,ZIndex=53},mTitle)

local mBtnBar = o("Frame",{
    Size=UDim2.new(1,0,0,34),Position=UDim2.new(0,0,1,-34),
    BackgroundTransparency=1,BorderSizePixel=0,ZIndex=53,
},mbox)
o("UIListLayout",{
    FillDirection=Enum.FillDirection.Horizontal,
    HorizontalAlignment=Enum.HorizontalAlignment.Center,
    VerticalAlignment=Enum.VerticalAlignment.Center,
    Padding=UDim.new(0,6),SortOrder=Enum.SortOrder.LayoutOrder,
},mBtnBar)

local function mBtn(txt,lo)
    local b=o("TextButton",{
        Size=UDim2.new(0,62,0,22),BackgroundColor3=D,BorderSizePixel=0,
        Text=txt,TextColor3=W,TextSize=10,Font=Enum.Font.SourceSansBold,
        AutoButtonColor=false,LayoutOrder=lo,ZIndex=54,
    },mBtnBar)
    stroke(b,1)
    b.MouseEnter:Connect(function() b.BackgroundColor3=Color3.fromRGB(30,30,30) end)
    b.MouseLeave:Connect(function() b.BackgroundColor3=D end)
    return b
end
local mCopy   = mBtn("Copiar",  1)
local mCancel = mBtn("Cancelar",2)
local mExec   = mBtn("Executar",3)

local _mCb,_mTxt = nil,nil
local function closeModal()
    modal.Visible=false _mCb=nil _mTxt=nil
end
local function openModal(displayTxt,copyTxt,cb)
    _mCb=cb _mTxt=copyTxt
    mTitle.Text=displayTxt
    modal.Visible=true
end

modalBg.MouseButton1Click:Connect(closeModal)
mCancel.MouseButton1Click:Connect(closeModal)
mExec.MouseButton1Click:Connect(function()
    local cb=_mCb closeModal()
    if cb then pcall(cb) end
end)
mCopy.MouseButton1Click:Connect(function()
    if _mTxt and setclipboard then pcall(setclipboard,_mTxt) end
    closeModal()
end)

-- ── Estado das abas ───────────────────────────────────────────────────
local tabBtns,tabPages,cur={},{},nil
local function sw(idx)
    if cur==idx then return end
    if cur then tabBtns[cur].BackgroundColor3=B tabBtns[cur].TextColor3=DT end
    cur=idx
    tabBtns[idx].BackgroundColor3=D tabBtns[idx].TextColor3=W
    for j,p in ipairs(tabPages) do p.Visible=(j==idx) end
end

-- ── Aba Main ──────────────────────────────────────────────────────────
do
    local i = 1
    local tb = o("TextButton",{
        Size=UDim2.new(1,0,0,28), BackgroundColor3=D, BorderSizePixel=0,
        Text="Main", TextColor3=W, TextSize=10,
        Font=Enum.Font.SourceSansBold, AutoButtonColor=false,
        LayoutOrder=i, TextWrapped=true,
    },tlist)
    o("Frame",{Size=UDim2.new(1,0,0,1),Position=UDim2.new(0,0,1,-1),BackgroundColor3=G,BorderSizePixel=0},tb)
    local pg = o("Frame",{
        Size=UDim2.new(1,0,1,0), BackgroundColor3=B, BorderSizePixel=0, Visible=true,
    },area)
    tabBtns[i]=tb tabPages[i]=pg cur=i
    tb.MouseButton1Click:Connect(function() sw(i) end)

    -- Layout responsivo: divide em coluna esquerda (avatar) e direita (info)
    -- Usa UDim2 relativo para se adaptar a qualquer tamanho de janela

    -- Coluna esquerda: avatar quadrado, ocupa ~38% da largura, centralizado verticalmente
    local leftCol = o("Frame",{
        Size=UDim2.new(0.38,0,1,0),
        Position=UDim2.new(0,0,0,0),
        BackgroundTransparency=1, BorderSizePixel=0,
    },pg)

    local avBox = o("Frame",{
        Size=UDim2.new(0.72,0,0,0),  -- largura relativa ao leftCol
        Position=UDim2.new(0.14,0,0.08,0),
        BackgroundColor3=B, BorderSizePixel=0, ClipsDescendants=true,
    },leftCol)
    -- Mantém aspect ratio quadrado via SizeConstraint
    avBox.AutomaticSize = Enum.AutomaticSize.None
    -- Usamos AbsoluteSize depois do layout para forçar quadrado
    local avImg = o("ImageLabel",{
        Size=UDim2.new(1,0,1,0),
        BackgroundTransparency=1, Image="", ScaleType=Enum.ScaleType.Crop,
        Name="AvImg",
    },avBox)

    -- Força avBox a ser quadrado baseado na largura
    local function fixAvSize()
        local w = avBox.AbsoluteSize.X
        if w > 0 then
            avBox.Size = UDim2.new(0.72, 0, 0, w)
        end
    end
    avBox:GetPropertyChangedSignal("AbsoluteSize"):Connect(fixAvSize)
    task.defer(fixAvSize)

    stroke(avBox,1)

    task.spawn(function()
        local ok,t = pcall(function()
            return game:GetService("Players"):GetUserThumbnailAsync(
                LP.UserId, Enum.ThumbnailType.AvatarBust, Enum.ThumbnailSize.Size420x420)
        end)
        if ok then avImg.Image=t end
    end)

    -- "Welcome, Name" abaixo do avatar
    local welcomeF = o("Frame",{
        Size=UDim2.new(1,0,0,44),
        Position=UDim2.new(0,0,1,-48),
        BackgroundTransparency=1, BorderSizePixel=0,
    },leftCol)
    o("UIListLayout",{
        SortOrder=Enum.SortOrder.LayoutOrder,
        HorizontalAlignment=Enum.HorizontalAlignment.Center,
        VerticalAlignment=Enum.VerticalAlignment.Center,
        Padding=UDim.new(0,1),
    },welcomeF)
    local wSize = math.max(10, math.floor(14 * math.max(0.5, _sc)))
    for k,row in ipairs({"Welcome,", LP.Name}) do
        o("TextLabel",{
            Size=UDim2.new(1,0,0,math.floor(22 * math.max(0.5,_sc))),
            BackgroundTransparency=1, Text=row,
            TextColor3=W, TextSize=wSize, Font=Enum.Font.Antique,
            TextXAlignment=Enum.TextXAlignment.Center,
            TextWrapped=true, LayoutOrder=k,
        },welcomeF)
    end

    -- Coluna direita: info em lista, ScrollingFrame para caber em telas pequenas
    local rightCol = o("ScrollingFrame",{
        Size=UDim2.new(0.62,-8,1,-8),
        Position=UDim2.new(0.38,4,0,4),
        BackgroundTransparency=1, BorderSizePixel=0,
        ScrollBarThickness=2, ScrollBarImageColor3=G,
        CanvasSize=UDim2.new(0,0,0,0),
        AutomaticCanvasSize=Enum.AutomaticSize.Y,
        ClipsDescendants=true,
    },pg)
    o("UIListLayout",{
        SortOrder=Enum.SortOrder.LayoutOrder,
        Padding=UDim.new(0,3),
    },rightCol)
    o("UIPadding",{
        PaddingTop=UDim.new(0,6),
        PaddingLeft=UDim.new(0,4),
        PaddingBottom=UDim.new(0,6),
    },rightCol)

    local joinStr = "N/A"
    pcall(function()
        local ts = os.time() - LP.AccountAge*86400
        local d  = os.date("*t",ts)
        local mo = {"January","February","March","April","May","June",
                    "July","August","September","October","November","December"}
        joinStr  = mo[d.month].." "..d.day.." "..d.year
    end)
    local platStr = UIS.TouchEnabled and "Android" or "PC"
    local devStr  = UIS.TouchEnabled and "Mobile"  or "Desktop"

    local tSize = math.max(9, math.floor(11 * math.max(0.55, _sc)))
    local rows = {
        {"Display:",   LP.DisplayName},
        {"Username:",  LP.Name},
        {"User ID:",   tostring(LP.UserId)},
        {"Age:",       LP.AccountAge.." days"},
        {"Joined:",    joinStr},
        {"Platform:",  platStr},
        {"Device:",    devStr},
    }
    for k, row in ipairs(rows) do
        local rf = o("Frame",{
            Size=UDim2.new(1,0,0,math.max(14, math.floor(16*math.max(0.55,_sc)))),
            BackgroundTransparency=1, BorderSizePixel=0,
            LayoutOrder=k,
        },rightCol)
        -- Label (key)
        o("TextLabel",{
            Size=UDim2.new(0.42,0,1,0),
            BackgroundTransparency=1, Text=row[1],
            TextColor3=DT, TextSize=tSize,
            Font=Enum.Font.Code,
            TextXAlignment=Enum.TextXAlignment.Left,
        },rf)
        -- Value
        o("TextLabel",{
            Size=UDim2.new(0.58,0,1,0),
            Position=UDim2.new(0.42,0,0,0),
            BackgroundTransparency=1, Text=row[2],
            TextColor3=W, TextSize=tSize,
            Font=Enum.Font.Code,
            TextXAlignment=Enum.TextXAlignment.Left,
            TextWrapped=true,
        },rf)
    end
end

-- ── Aba Executor ─────────────────────────────────────────────────────
do
    local i = 2
    local tb = o("TextButton",{
        Size=UDim2.new(1,0,0,28), BackgroundColor3=B, BorderSizePixel=0,
        Text="Executor", TextColor3=DT, TextSize=10,
        Font=Enum.Font.SourceSansBold, AutoButtonColor=false,
        LayoutOrder=i, TextWrapped=true,
    },tlist)
    o("Frame",{Size=UDim2.new(1,0,0,1),Position=UDim2.new(0,0,1,-1),BackgroundColor3=G,BorderSizePixel=0},tb)
    local pg = o("Frame",{
        Size=UDim2.new(1,0,1,0), BackgroundColor3=B, BorderSizePixel=0, Visible=false,
    },area)
    tabBtns[i]=tb tabPages[i]=pg
    tb.MouseButton1Click:Connect(function() sw(i) end)

    local editorFrame = o("Frame",{
        Size=UDim2.new(1,-16,1,-48), Position=UDim2.new(0,8,0,8),
        BackgroundColor3=B, BorderSizePixel=0,
    },pg)
    stroke(editorFrame,1)
    local editor = o("TextBox",{
        Size=UDim2.new(1,-8,1,-8), Position=UDim2.new(0,4,0,4),
        BackgroundTransparency=1,
        Text="", PlaceholderText="-- Write your script here",
        PlaceholderColor3=Color3.fromRGB(80,80,80),
        TextColor3=W, TextSize=12, Font=Enum.Font.Code,
        TextXAlignment=Enum.TextXAlignment.Left,
        TextYAlignment=Enum.TextYAlignment.Top,
        ClearTextOnFocus=false, MultiLine=true, TextWrapped=true,
    },editorFrame)

    local btnBar = o("Frame",{
        Size=UDim2.new(1,-16,0,30), Position=UDim2.new(0,8,1,-38),
        BackgroundTransparency=1, BorderSizePixel=0,
    },pg)
    o("UIListLayout",{
        FillDirection=Enum.FillDirection.Horizontal,
        HorizontalAlignment=Enum.HorizontalAlignment.Right,
        VerticalAlignment=Enum.VerticalAlignment.Center,
        Padding=UDim.new(0,6), SortOrder=Enum.SortOrder.LayoutOrder,
    },btnBar)

    local function makeBtn(txt, lo)
        local b = o("TextButton",{
            Size=UDim2.new(0,70,0,22), BackgroundColor3=B, BorderSizePixel=0,
            Text=txt, TextColor3=W, TextSize=10, Font=Enum.Font.SourceSansBold,
            AutoButtonColor=false, LayoutOrder=lo,
        },btnBar)
        stroke(b,1)
        b.MouseEnter:Connect(function() b.BackgroundColor3=D end)
        b.MouseLeave:Connect(function() b.BackgroundColor3=B end)
        return b
    end

    local copyBtn  = makeBtn("Copy",    1)
    local clearBtn = makeBtn("Clear",   2)
    local execBtnE = makeBtn("Execute", 3)

    copyBtn.MouseButton1Click:Connect(function()
        if setclipboard then pcall(setclipboard, editor.Text) end
    end)
    clearBtn.MouseButton1Click:Connect(function()
        editor.Text = ""
    end)
    execBtnE.MouseButton1Click:Connect(function()
        local code = editor.Text
        if code ~= "" then
            local fn, err = loadstring(code)
            if fn then pcall(fn)
            else warn("[Executor] "..tostring(err)) end
        end
    end)
end

-- ── API pública ───────────────────────────────────────────────────────
function Hub:Tab(name)
    local i=#tabBtns+1
    local tb=o("TextButton",{
        Size=UDim2.new(1,0,0,28),
        BackgroundColor3=B,BorderSizePixel=0,
        Text=name,TextColor3=DT,TextSize=10,
        Font=Enum.Font.SourceSansBold,AutoButtonColor=false,
        LayoutOrder=i,TextWrapped=true,
    },tlist)
    o("Frame",{Size=UDim2.new(1,0,0,1),Position=UDim2.new(0,0,1,-1),BackgroundColor3=G,BorderSizePixel=0},tb)

    local pg=o("Frame",{Size=UDim2.new(1,0,1,0),BackgroundColor3=B,BorderSizePixel=0,Visible=false},area)

    local hotbar=o("Frame",{Size=UDim2.new(1,0,0,20),BackgroundColor3=B,BorderSizePixel=0},pg)

    local searchW=120
    local search=o("TextBox",{
        Size=UDim2.new(0,searchW,1,0),BackgroundColor3=B,BorderSizePixel=0,
        PlaceholderText="Search Script",PlaceholderColor3=Color3.fromRGB(90,90,90),
        Text="",TextColor3=W,TextSize=11,Font=Enum.Font.SourceSans,
        ClearTextOnFocus=false,TextXAlignment=Enum.TextXAlignment.Left,
    },hotbar)
    o("UIPadding",{PaddingLeft=UDim.new(0,6)},search)

    local countLbl=o("TextLabel",{
        Size=UDim2.new(1,-searchW-8,1,0),Position=UDim2.new(0,searchW+6,0,0),
        BackgroundTransparency=1,Text="Scripts: 0",TextColor3=DT,
        TextSize=10,Font=Enum.Font.SourceSans,TextXAlignment=Enum.TextXAlignment.Right,
    },hotbar)
    o("UIPadding",{PaddingRight=UDim.new(0,6)},countLbl)

    local GAP=6 local COLS=6 local ROWH=28
    local scroll=o("ScrollingFrame",{
        Size=UDim2.new(1,0,1,-20),Position=UDim2.new(0,0,0,20),
        BackgroundTransparency=1,BorderSizePixel=0,
        ScrollBarThickness=2,ScrollBarImageColor3=G,
        CanvasSize=UDim2.new(0,0,0,0),AutomaticCanvasSize=Enum.AutomaticSize.Y,
    },pg)
    o("UIPadding",{
        PaddingTop=UDim.new(0,GAP),PaddingLeft=UDim.new(0,GAP),
        PaddingRight=UDim.new(0,GAP),PaddingBottom=UDim.new(0,GAP),
    },scroll)
    o("UIGridLayout",{
        CellSize=UDim2.new(1/COLS,-(GAP*(COLS-1))/COLS,0,ROWH),
        CellPadding=UDim2.new(0,GAP,0,GAP),
        SortOrder=Enum.SortOrder.LayoutOrder,
        HorizontalAlignment=Enum.HorizontalAlignment.Left,
    },scroll)

    tabBtns[i]=tb tabPages[i]=pg
    tb.MouseButton1Click:Connect(function() sw(i) end)

    local allBtns={}
    local function updateCount()
        local v=0 for _,b in ipairs(allBtns) do if b.Visible then v+=1 end end
        countLbl.Text="Scripts: "..v
    end
    search:GetPropertyChangedSignal("Text"):Connect(function()
        local q=search.Text:lower()
        for _,b in ipairs(allBtns) do
            b.Visible=(q=="" or b.Name:lower():find(q,1,true)~=nil)
        end
        updateCount()
    end)

    local T={}
    function T:Button(txt,url)
        local b=o("TextButton",{
            Name=txt,BackgroundColor3=D,BorderSizePixel=0,
            Text=txt,TextColor3=W,TextSize=10,Font=Enum.Font.SourceSans,
            AutoButtonColor=false,TextWrapped=true,LineHeight=1.1,
        },scroll)
        stroke(b,1)
        b.MouseEnter:Connect(function() b.BackgroundColor3=Color3.fromRGB(25,25,25) end)
        b.MouseLeave:Connect(function() b.BackgroundColor3=D end)
        local copyTxt='loadstring(game:HttpGet("'..url..'"))()'
        b.MouseButton1Click:Connect(function()
            openModal(txt,copyTxt,function() loadstring(game:HttpGet(url))() end)
        end)
        table.insert(allBtns,b)
        countLbl.Text="Scripts: "..#allBtns
        return self
    end
    return T
end

-- ── Pill ──────────────────────────────────────────────────────────────
local pill=o("Frame",{
    Name="Pill",Size=UDim2.new(0,54,0,18),Position=UDim2.new(1,-60,1,-400),
    BackgroundColor3=B,BorderSizePixel=0,Visible=false,ZIndex=200,
},sg)
stroke(pill,1)
_G.UniversalHubPill=pill
local pillBtn=o("TextButton",{
    Size=UDim2.new(1,0,1,0),BackgroundTransparency=1,
    Text="> hub",TextSize=9,Font=Enum.Font.SourceSansBold,
    TextColor3=W,BorderSizePixel=0,ZIndex=201,
},pill)
pillBtn.MouseButton1Click:Connect(function() win.Visible=true pill.Visible=false end)

UIS.InputBegan:Connect(function(inp,gp)
    if not gp and inp.KeyCode==Enum.KeyCode.RightShift then
        local v=not win.Visible win.Visible=v
        if v then pill.Visible=false end
    end
end)

return Hub
