--══════════════════════════════════════════════════════
--  KronUI Loader + Multi Fling
--══════════════════════════════════════════════════════

local Players           = game:GetService("Players")
local RunService         = game:GetService("RunService")
local ReplicatedStorage  = game:GetService("ReplicatedStorage")
local Player             = Players.LocalPlayer

local LIB_URL    = "https://raw.githubusercontent.com/FabJheiny/Scripts/refs/heads/main/KronUI"
local PILL_IMAGE = "rbxassetid://100932674765762"

local ok, result = pcall(function()
    return loadstring(game:HttpGet(LIB_URL))()
end)
if not ok then
    warn("[KronUI] Falha ao carregar a lib: " .. tostring(result))
    return
end
local KronUI = result
local ui = KronUI.new("Multi Fling", PILL_IMAGE)

-- ══════════════════════════════════════════════
--  ESTADO
-- ══════════════════════════════════════════════
local AntiFlingActive  = false
local TouchFlingActive = false
local antiFlingTracked = {}
local antiFlingConnections = {}
local antiFlingRenderConn  = nil
local SelectedTargets = {}
local FlingActive     = false
getgenv().OldPos = nil
getgenv().FPDH   = workspace.FallenPartsDestroyHeight

-- ══════════════════════════════════════════════
--  ANTI-FLING
-- ══════════════════════════════════════════════
local function disableCanCollide(part)
    if part:IsA("BasePart") and part.CanCollide then part.CanCollide = false end
end
local function trackAFCharacter(character)
    for _, part in pairs(character:GetChildren()) do disableCanCollide(part) end
    local conn = character.ChildAdded:Connect(function(child) disableCanCollide(child) end)
    table.insert(antiFlingConnections, conn)
end
local function trackAFPlayer(player)
    if player == Player then return end
    if player.Character then trackAFCharacter(player.Character) end
    local conn = player.CharacterAdded:Connect(trackAFCharacter)
    table.insert(antiFlingConnections, conn)
    antiFlingTracked[player] = true
end
local function startAntiFling()
    antiFlingTracked = {}; antiFlingConnections = {}
    for _, player in pairs(Players:GetPlayers()) do trackAFPlayer(player) end
    table.insert(antiFlingConnections, Players.PlayerAdded:Connect(trackAFPlayer))
    antiFlingRenderConn = RunService.RenderStepped:Connect(function()
        for player in pairs(antiFlingTracked) do
            if player.Character then
                for _, part in pairs(player.Character:GetChildren()) do disableCanCollide(part) end
            end
        end
    end)
end
local function stopAntiFling()
    for _, conn in ipairs(antiFlingConnections) do conn:Disconnect() end
    antiFlingConnections = {}; antiFlingTracked = {}
    if antiFlingRenderConn then antiFlingRenderConn:Disconnect(); antiFlingRenderConn = nil end
end

-- ══════════════════════════════════════════════
--  TOUCH FLING
-- ══════════════════════════════════════════════
local function runTouchFling()
    local lp = Players.LocalPlayer
    local movel = 0.1
    while TouchFlingActive do
        RunService.Heartbeat:Wait()
        local c   = lp.Character
        local hrp = c and c:FindFirstChild("HumanoidRootPart")
        if hrp then
            local vel = hrp.Velocity
            hrp.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)
            RunService.RenderStepped:Wait()
            hrp.Velocity = vel
            RunService.Stepped:Wait()
            hrp.Velocity = vel + Vector3.new(0, movel, 0)
            movel = -movel
        end
    end
end

-- ══════════════════════════════════════════════
--  SKID FLING
-- ══════════════════════════════════════════════
local function SkidFling(TargetPlayer)
    local Character = Player.Character
    local Humanoid  = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart  = Humanoid  and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter then return end
    local THumanoid, TRootPart, THead, Accessory, Handle
    if TCharacter:FindFirstChildOfClass("Humanoid") then THumanoid = TCharacter:FindFirstChildOfClass("Humanoid") end
    if THumanoid and THumanoid.RootPart then TRootPart = THumanoid.RootPart end
    if TCharacter:FindFirstChild("Head")             then THead     = TCharacter.Head end
    if TCharacter:FindFirstChildOfClass("Accessory") then Accessory = TCharacter:FindFirstChildOfClass("Accessory") end
    if Accessory and Accessory:FindFirstChild("Handle") then Handle = Accessory.Handle end
    if Character and Humanoid and RootPart then
        if RootPart.Velocity.Magnitude < 50 then getgenv().OldPos = RootPart.CFrame end
        if THumanoid and THumanoid.Sit then return end
        if THead        then workspace.CurrentCamera.CameraSubject = THead
        elseif Handle   then workspace.CurrentCamera.CameraSubject = Handle
        elseif THumanoid and TRootPart then workspace.CurrentCamera.CameraSubject = THumanoid end
        if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end
        local FPos = function(BasePart, Pos, Ang)
            RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
            Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
            RootPart.Velocity    = Vector3.new(9e7, 9e7*10, 9e7)
            RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        end
        local SFBasePart = function(BasePart)
            local TimeToWait = 2; local Time = tick(); local Angle = 0
            repeat
                if RootPart and THumanoid then
                    if TouchFlingActive then
                        local hrp = Character:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            local vel = hrp.Velocity
                            hrp.Velocity = vel*10000+Vector3.new(0,10000,0)
                            RunService.RenderStepped:Wait()
                            hrp.Velocity = vel; RunService.Stepped:Wait()
                        end
                        Angle += 100
                        FPos(BasePart,CFrame.new(0,1.5,0)+THumanoid.MoveDirection*BasePart.Velocity.Magnitude/1.25,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0)+THumanoid.MoveDirection*BasePart.Velocity.Magnitude/1.25,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                    elseif BasePart.Velocity.Magnitude < 50 then
                        Angle += 100
                        FPos(BasePart,CFrame.new(0,1.5,0)+THumanoid.MoveDirection*BasePart.Velocity.Magnitude/1.25,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0)+THumanoid.MoveDirection*BasePart.Velocity.Magnitude/1.25,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,1.5,0)+THumanoid.MoveDirection*BasePart.Velocity.Magnitude/1.25,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0)+THumanoid.MoveDirection*BasePart.Velocity.Magnitude/1.25,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,1.5,0)+THumanoid.MoveDirection,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0)+THumanoid.MoveDirection,CFrame.Angles(math.rad(Angle),0,0)) task.wait()
                    else
                        FPos(BasePart,CFrame.new(0,1.5,THumanoid.WalkSpeed),CFrame.Angles(math.rad(90),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,-THumanoid.WalkSpeed),CFrame.Angles(0,0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,1.5,THumanoid.WalkSpeed),CFrame.Angles(math.rad(90),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0),CFrame.Angles(math.rad(90),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0),CFrame.Angles(0,0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0),CFrame.Angles(math.rad(90),0,0)) task.wait()
                        FPos(BasePart,CFrame.new(0,-1.5,0),CFrame.Angles(0,0,0)) task.wait()
                    end
                end
            until Time+TimeToWait < tick() or not FlingActive
        end
        workspace.FallenPartsDestroyHeight = 0/0
        local BV = Instance.new("BodyVelocity")
        BV.Parent = RootPart; BV.Velocity = Vector3.new(0,0,0); BV.MaxForce = Vector3.new(9e9,9e9,9e9)
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
        if TRootPart   then SFBasePart(TRootPart)
        elseif THead   then SFBasePart(THead)
        elseif Handle  then SFBasePart(Handle)
        else return end
        BV:Destroy()
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        workspace.CurrentCamera.CameraSubject = Humanoid
        if getgenv().OldPos then
            repeat
                RootPart.CFrame = getgenv().OldPos * CFrame.new(0,.5,0)
                Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0,.5,0))
                Humanoid:ChangeState("GettingUp")
                for _, part in pairs(Character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.Velocity = Vector3.new(); part.RotVelocity = Vector3.new()
                    end
                end
                task.wait()
            until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25
            workspace.FallenPartsDestroyHeight = getgenv().FPDH
        end
    end
end

-- ══════════════════════════════════════════════
--  START / STOP
-- ══════════════════════════════════════════════
local function GetSelectedList()
    local list = {}
    for name, plr in pairs(SelectedTargets) do
        if plr and plr.Parent then table.insert(list, plr)
        else SelectedTargets[name] = nil end
    end
    return list
end

local function StartFling()
    if FlingActive then return end
    if #GetSelectedList() == 0 then return end
    FlingActive = true
    task.spawn(function()
        while FlingActive do
            for _, player in pairs(GetSelectedList()) do
                if not FlingActive then break end
                SkidFling(player)
                task.wait(0.1)
            end
            task.wait(0.5)
        end
    end)
end
local function StopFling()
    FlingActive = false
end

-- ══════════════════════════════════════════════
--  UI (KronUI)
-- ══════════════════════════════════════════════
local TargetsDropdown = nil
local allTargetFrames = {}
local uiReady         = false

local function GetPlayerNames()
    local names = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Player then table.insert(names, p.Name) end
    end
    table.sort(names)
    return names
end

local function onTargetSelection(sel)
    SelectedTargets = {}
    for _, name in ipairs(sel) do
        local plr = Players:FindFirstChild(name)
        if plr then SelectedTargets[name] = plr end
    end
end

local function rebuildTargetsDropdown()
    if not ui then return end

    local savedOrder = nil
    if TargetsDropdown and TargetsDropdown.Frame and TargetsDropdown.Frame.Parent then
        savedOrder = TargetsDropdown.Frame.LayoutOrder
    end

    for _, frame in ipairs(allTargetFrames) do
        if frame and frame.Parent then frame:Destroy() end
    end
    table.clear(allTargetFrames)
    TargetsDropdown = nil

    local playerNames = GetPlayerNames()
    local nameSet = {}
    for _, n in ipairs(playerNames) do nameSet[n] = true end
    for name in pairs(SelectedTargets) do
        if not nameSet[name] then SelectedTargets[name] = nil end
    end

    local validSelected = {}
    for name in pairs(SelectedTargets) do
        table.insert(validSelected, name)
    end

    TargetsDropdown = ui:AddMultiDropdown(
        "Select Players", playerNames, validSelected, onTargetSelection
    )

    if TargetsDropdown and TargetsDropdown.Frame then
        table.insert(allTargetFrames, TargetsDropdown.Frame)
        if savedOrder then
            TargetsDropdown.Frame.LayoutOrder = savedOrder
        end
    end
end

local rebuildPending = false
local function scheduleRebuild()
    if not uiReady or rebuildPending then return end
    rebuildPending = true
    task.delay(0.3, function()
        rebuildPending = false
        rebuildTargetsDropdown()
    end)
end

-- ── Construção da UI ─────────────────────────
ui:AddSectionLabel("Multi Fling")

TargetsDropdown = ui:AddMultiDropdown("Select Players", GetPlayerNames(), {}, onTargetSelection)
if TargetsDropdown and TargetsDropdown.Frame then
    table.insert(allTargetFrames, TargetsDropdown.Frame)
end

ui:AddButton("Refresh List", function()
    task.defer(rebuildTargetsDropdown)
end)

ui:AddButton("Select All", function()
    SelectedTargets = {}
    for _, name in ipairs(GetPlayerNames()) do
        local plr = Players:FindFirstChild(name)
        if plr then SelectedTargets[name] = plr end
    end
    task.defer(rebuildTargetsDropdown)
end)

ui:AddButton("Select None", function()
    SelectedTargets = {}
    task.defer(rebuildTargetsDropdown)
end)

ui:AddButton("Start Fling", function() StartFling() end)
ui:AddButton("Stop Fling",  function() StopFling()  end)

ui:AddDivider()
ui:AddSectionLabel("Extras")

ui:AddToggle("Anti-Fling", false, function(state)
    AntiFlingActive = state
    if state then startAntiFling() else stopAntiFling() end
end)

ui:AddToggle("Touch Fling", false, function(state)
    TouchFlingActive = state
    if state then
        if not ReplicatedStorage:FindFirstChild("Kron") then
            local d = Instance.new("Decal")
            d.Name = "Kron"; d.Parent = ReplicatedStorage
        end
        coroutine.resume(coroutine.create(runTouchFling))
    end
end)

uiReady = true

-- ── Auto-refresh quando jogadores entram/saem ──
Players.PlayerAdded:Connect(scheduleRebuild)
Players.PlayerRemoving:Connect(function(player)
    SelectedTargets[player.Name] = nil
    task.delay(0.1, scheduleRebuild)
end)
